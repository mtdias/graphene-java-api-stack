plugins {
    id 'java-gradle-plugin'
}

gradlePlugin {
    plugins {
        addDep {
            id = 'com.stackspot.graphene.add_dep'
            implementationClass = 'com.stackspot.graphene.AddDepPlugin'
        }
    }
}
